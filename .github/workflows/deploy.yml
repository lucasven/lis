nname: Deploy Lis

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Publish
        env:
          DOTNET_ROOT: /home/agenticcompany/.dotnet
        run: |
          export PATH="$DOTNET_ROOT:$PATH"
          dotnet publish Lis.Api/Lis.Api.csproj -c Release -o /home/agenticcompany/lis/publish

      - name: Restart service
        run: |
          nohup bash -c 'sleep 3 && sudo systemctl restart lis' &>/dev/null &
          echo "Restart scheduled in background"

      - name: Health check
        run: |
          sleep 10
          curl -sf http://localhost:3010/health || (sudo journalctl -u lis --no-pager -n 30 && exit 1)
